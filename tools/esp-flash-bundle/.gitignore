# Artefacts reconstruits à chaque déploiement par .github/workflows/pages.yml (npm ci +
# npm run build) : seuls package.json et package-lock.json sont la source de vérité versionnée.
node_modules/
dist/
